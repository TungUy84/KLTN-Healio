import React from 'react';
import { View } from 'react-native';

// File này chỉ tồn tại để hiện nút (+) trên thanh Menu
// Khi bấm vào, sự kiện đã bị chặn ở _layout.tsx để mở trang Search
export default function AddMealPlaceholder() {
  return <View />;
}